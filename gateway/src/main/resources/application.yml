server:
  port: 8080

spring:
  cloud:
    gateway:
      default-filters:
      - StripPrefix=1

      routes:
      - predicates:
        - Path=/user/**
        uri: lb://user-client
      - predicates:
        - Path=/article/**
        uri: lb://article-client
