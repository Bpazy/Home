server:
  port: 8080

spring:
  cloud:
    gateway:
      default-filters:
      - StripPrefix=1

      routes:
      - predicates:
        - Path=/user/**
        uri: lb://user-service
      - predicates:
        - Path=/article/**
        uri: lb://article-service
